package creditcard;

public class Ar_UListCC_Driver {
public static void main(String args[]) throws Exception{
Ar_UListCC testList = new AR_UListCC();
scanner rewScanner = new scanner(new File("src/creditcard_balanced.csv"));
sanner colScanner;
rowScanner.nextLine(){
	while )rowScanner.hasNextLine()){
		string rowString = rowScanner.nextLine(){
			colScanner = new scanner(rowScanner);
			colScanner.useDelimiter(",");
			int newtime = Integer.parseInt(colScanner.next());
			double newamount = double.parseint(colScanner.next());
			int newClass = Integer.parseInt(colScanner.next());
			CCFraudrecord newREc = new CCFraudRecord(Newtime, newamount)
					testList.putitem(newRecc);
			}
	}
}
}
